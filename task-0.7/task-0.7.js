{
  function celsiusToFahrenheit(num) {
    var answer = num * (9 / 5) + 32;
    return answer;
  }

  function fahrenheitToCelsius(num) {
    var answer = (num - 32) * (5 / 9);
    return answer;
  }
}
